<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/Background</item>
    </style>

    <style name="TitleButton" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@color/colorPrimary</item>
        <item name="colorControlHighlight">@color/TitleButtonClicked</item>
    </style>

    <style name="NormalButton" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@color/Button</item>
        <item name="colorControlHighlight">@color/NormalButtonClicked</item>
    </style>

</resources>
